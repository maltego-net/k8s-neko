apiVersion: v1
kind: ConfigMap
metadata:
  name: neko-server-config 
  namespace: neko
data:
  NODE_ENV: "production"
  PORT: "5000"
  CLIENT_URL: "http://neko-client-service"
  MONGO_HOST: "mongodb-service"
  MONGO_PORT: "27017"
  MONGO_DB: "neko"